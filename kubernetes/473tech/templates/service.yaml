apiVersion: v1
kind: Service
metadata:
  name: {{ include "473tech.fullname" . }}
  labels:
    {{- include "473tech.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "473tech.selectorLabels" . | nindent 4 }}
